import { Center } from "@chakra-ui/react";
import waiting from "../assets/Waiting.gif";

const Waiting = () => {
  return (
    <><Center><img id="Waiting" src={waiting} alt="Waiting" /></Center></>
  );
};

export default Waiting;